package io.rsocket.transport.quic;

/**
 * QUIC Constants
 *
 * @author linux_china
 */
public interface QuicConstants {
    byte[] proto = new byte[]{0x08, 'r', 's', 'o', 'c', 'k', 'e', 't', '/', '1', '.', '0'};
}
